# Release State

This directory tracks the latest successfully released PyTorch wheel build.

- `latest.json` — Current release state. Updated automatically on each successful release run and committed back to the repository.
- `history/` — Per-release snapshots named after the release tag, kept for auditing.

Do not edit these files manually.
